name: hello
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo "A demo build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "test phase"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy the app
        run: echo "delopy to k8s
